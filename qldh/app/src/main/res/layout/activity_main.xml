<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/textAppName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ứng dụng quản lý đơn hàng"
        android:textSize="24sp"
        android:textStyle="bold"/>

    <Button
        android:id="@+id/buttonAddOrder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nhập đơn hàng"
        android:layout_below="@id/textAppName"
        android:layout_marginTop="20dp"/>

    <Button
        android:id="@+id/buttonShowOrders"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hiển thị tất cả đơn hàng"
        android:layout_below="@id/buttonAddOrder"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/buttonCalculateTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tính tổng"
        android:layout_below="@id/buttonShowOrders"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
